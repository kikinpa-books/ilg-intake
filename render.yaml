services:
  - type: web
    name: ilg-intake
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
